<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.likelion.bebc25.sns.mapper.MemberMapper">

    <select id="findByEmail" parameterType="string" resultType="net.likelion.bebc25.sns.domain.Member">
        SELECT id, email, password, nickname, profile_image, role, created_at FROM member WHERE email = #{email}
    </select>

    <select id="findById" parameterType="long" resultType="net.likelion.bebc25.sns.domain.Member">
        SELECT id, email, password, nickname, profile_image, role, created_at FROM member WHERE id = #{id}
    </select>

    <!-- 신규 회원 등록 (소셜 로그인 자동 회원가입) -->
    <insert id="save" parameterType="net.likelion.bebc25.sns.domain.Member" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO member (email, password, nickname, role, created_at)
        VALUES (#{email}, COALESCE(#{password}, ''), #{nickname}, #{role}, NOW())
    </insert>

</mapper>


