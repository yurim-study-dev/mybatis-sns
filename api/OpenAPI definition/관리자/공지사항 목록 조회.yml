info:
  name: 공지사항 목록 조회
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/notices"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
