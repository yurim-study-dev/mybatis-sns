info:
  name: 마이페이지
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/members/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
